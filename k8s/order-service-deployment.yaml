apiVersion: apps/v1
kind: Deployment
metadata:
  name: lugx-order
spec:
  replicas: 2
  selector:
    matchLabels:
      app: lugx-order
  template:
    metadata:
      labels:
        app: lugx-order
    spec:
      containers:
      - name: lugx-order
        image: 476287788277.dkr.ecr.ap-south-1.amazonaws.com/lugx-order-service:latest
        ports:
        - containerPort: 3002
        env:
        - name: DB_HOST
          value: postgres
        - name: DB_PORT
          value: "5432"
        - name: DB_NAME
          value: lugx-gaming
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: db-secret
              key: username
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-secret
              key: password